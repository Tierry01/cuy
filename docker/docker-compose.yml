version: '3.4'
services:
  secelged-share:
    image: alfresco-share-secelged:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../secelged-share-docker/target
    environment:
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
      REPO_HOST: secelged-acs
      REPO_PORT: 8080
    ports:
      - "${share.port}:8080"
      - "${share.debug.port}:8888"
  secelged-acs:
    image: alfresco-content-services-secelged:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../secelged-platform-docker/target
    environment:
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
    ports:
      - "${acs.port}:8080"
      - "${acs.debug.port}:8888"
    volumes:
      - secelged-acs-volume:/usr/local/tomcat/alf_data
    depends_on:
      - secelged-postgres
  secelged-postgres:
    image: postgres:9.6
    environment:
      POSTGRES_DB: alfresco
      POSTGRES_USER: alfresco
      POSTGRES_PASSWORD: alfresco
    command: postgres -c max_connections=300 -c log_min_messages=LOG
    ports:
      - "${postgres.port}:5432"
    volumes:
      - secelged-db-volume:/var/lib/postgresql/data
  secelged-ass:
    image: alfresco/alfresco-search-services:1.2.0
    environment:
      SOLR_ALFRESCO_HOST: secelged-acs
      SOLR_ALFRESCO_PORT: 8080
      SOLR_SOLR_HOST: secelged-ass
      SOLR_SOLR_PORT: 8983
      SOLR_CREATE_ALFRESCO_DEFAULTS: alfresco,archive
    ports:
      - "8983:8983"
    volumes:
      - secelged-ass-volume:/opt/alfresco-search-services/contentstore
      - secelged-ass-volume:/opt/alfresco-search-services/data
volumes:
  secelged-acs-volume:
    external: true
  secelged-db-volume:
    external: true
  secelged-ass-volume:
    external: true